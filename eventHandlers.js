function handleSendCustomerReminder(customerName, customerEmail, customerPaymentDetails) {
    // send an email to the customer that their invoices will not be automatically paid
}

function handleSendFarewellEmail(customerName, customerEmail) {
    // send an email to the customer - wishing them all the best and inviting them back
}

module.exports = {
    handleSendCustomerReminder,
    handleSendFarewellEmail
}
